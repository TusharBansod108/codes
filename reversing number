class Solution {
public:
    int reverse(int x) {
        int r,sum=0;
        while(x>0 || x<0){
            r=x%10;
            sum=(sum * 10) + r;
            x=x/10;
        }
        return sum;
}
};
